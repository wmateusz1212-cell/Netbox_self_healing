name: NetDevOps Lab Automation

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  drift-detection:
    name: Network Drift Detection & Remediation
    runs-on: self-hosted
    steps:
      - name: Checkout Code
        run: cd /root && git pull origin main

      - name: Verify Netbox Connectivity
        run: curl -s http://localhost:8000/api/dcim/devices/ > /dev/null

      - name: Run Ansible Sync & Remediation
        env:
          ANSIBLE_SSH_PASS: ${{ secrets.ANSIBLE_SSH_PASS }}
        run: |
          ansible-playbook -i /root/netbox-docker/netbox_inventory.sh /root/netbox-docker/netdevops-lab/deploy_lab.yml
